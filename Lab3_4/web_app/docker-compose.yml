version: '3'

services:
  db:
    image: mysql:5.7
    ports:
      - 3306
    environment:
      MYSQL_DATABASE: 'db_django'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'password'
    restart: always
    volumes:
      - ./db_django:/var/lib/mysql
  web:
    image: divaon/blog_app:2.0
    command: sh -c "python manage.py runserver localhost:8000"
    environment:
      WAIT_HOSTS: mysql:3306
    volumes:
      - .:/web_django
    ports:
      - '7000:8000'
    depends_on:
      - db
